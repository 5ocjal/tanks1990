export class ArenaScene extends Phaser.Scene {
  colorGreen = "6ad373";

  constructor() {
    super({
      key: "ArenaScene",
    });
  }

  init() {
    this.cameras.main.setBackgroundColor(this.colorGreen);
  }

  preload() {}

  create() {
    this.create.
  }

  update() {}
}
